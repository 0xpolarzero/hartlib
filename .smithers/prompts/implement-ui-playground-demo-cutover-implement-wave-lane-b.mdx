# Implement wave lane B

Implement only lane B's assigned work items and tests under the frozen file
ownership. Read `AGENTS.md`, the settled plan, canonical docs, and the local
Effect v4 reference for backend work. Preserve retained bytes and unrelated
changes. Do not add mocks, placeholders, aliases, fallbacks, compatibility
layers, or a second chat path. Run real focused checks and report exact files,
deletions, tests, artifacts, and exceptions.

Assignment:

{JSON.stringify(props.assignment, null, 2)}

Frozen contracts:

{JSON.stringify(props.frozenContracts, null, 2)}

REQUIRED OUTPUT:
{props.schema}
