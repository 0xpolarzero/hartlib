# Recheck the repaired dashboard

Repeat the live read-only route and browser inspection at `{props.route}` after
the reported repairs. Verify the same Gateway data, agent chat streams,
envelopes, status distinctions, crash-resume views, blockers, approvals, and
six viewport widths. Compare the new evidence with the prior review and report
only findings that remain.

Prior review:

{JSON.stringify(props.priorReview, null, 2)}

REQUIRED OUTPUT:
{props.schema}
