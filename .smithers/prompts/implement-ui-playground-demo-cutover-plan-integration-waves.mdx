# Plan disjoint integration waves

Turn the frozen contracts into ordered work items and integration waves for
the package UI, demo and publisher reachability, visualization fixtures,
singular chat, migration 0074 and lifecycle behavior, cleanup, tests, and docs.
Assign exact disjoint file sets, dependency edges, sequential edges, and
parallel batches. Every path must have one owner or be explicitly shared by a
serialized edge. Keep `ui-playground/` out of every mutation set. No work item
may rely on a mock, placeholder, compatibility layer, or best-effort check.

Frozen contracts:

{JSON.stringify(props.frozenContracts, null, 2)}

REQUIRED OUTPUT:
{props.schema}
