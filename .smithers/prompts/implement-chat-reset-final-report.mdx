# Shape the final implementation report

Do not edit files, commit, push, publish, or open a pull request. Shape the
provided plan and iteration-aware outputs into the final structured report.
Use only recorded outputs; do not fabricate a changed file, test result,
review decision, protected-file state, or readiness claim. Include all
validation and review iterations, the three repair counts, foundation evidence,
the final changed-file list, and the exact blocker when readiness is not green.

## Recorded inputs

{JSON.stringify(props.input, null, 2)}

## Readiness

{JSON.stringify(props.readiness, null, 2)}

The workflow leaves successful edits uncommitted in the current working tree.

REQUIRED OUTPUT:
{props.schema}
