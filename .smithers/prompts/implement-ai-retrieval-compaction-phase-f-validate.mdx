# Validate Phase F

Do not edit files. Run the supplied Phase F command set exactly and in order,
capturing real output and exit codes. The set covers evaluation, phase logging,
shared-chat, API-client stream, web, and demo tests. Do not replace commands,
apply another migration, or hide failures.

## Commands

`{JSON.stringify(props.commands)}`

## Context

{JSON.stringify(props.context, null, 2)}

REQUIRED OUTPUT:
{props.schema}
