# Review Phase F and clean cutover

Do not edit files. Read Sections 14–17, canonical docs, the full current diff,
all phase outputs, final migration evidence, and the Effect v4 reference. Verify
evaluation v4 replay fields, deterministic provider cases, web/demo projections,
canonical contract accuracy, obsolete-code deletion, safe logs, protected state,
and absence of mixed producers or compatibility paths. Approve only with concrete
test and file evidence. List every blocker and do not create a review repair path.

## Plan and validation

{JSON.stringify(props.plan, null, 2)}

{JSON.stringify(props.validation, null, 2)}

REQUIRED OUTPUT:
{props.schema}
