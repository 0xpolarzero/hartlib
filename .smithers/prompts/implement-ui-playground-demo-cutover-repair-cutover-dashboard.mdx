# Repair dashboard review findings

Fix every valid finding from the live dashboard review in the workflow-owned UI
file only. Keep the shipped Gateway hook and component contracts. Preserve
distinct failed and skipped statuses, show all visible agent chat streams, and
keep output envelope parsing defensive. Do not read internals, hand-roll status
or table systems, add a new route, or change workflow behavior.

Findings:

{JSON.stringify(props.findings, null, 2)}

REQUIRED OUTPUT:
{props.schema}
