# Repair wave contract findings

Resolve every valid pre-integration ownership or contract finding within the
already assigned file set. Keep lane boundaries, retained bytes, deletion
rules, dependency limits, and settled plan decisions. Add or correct real tests
when the finding affects behavior. Do not broaden scope or introduce a
fallback, alias, compatibility layer, mock, or placeholder.

Review:

{JSON.stringify(props.review, null, 2)}

Assignment:

{JSON.stringify(props.assignment, null, 2)}

REQUIRED OUTPUT:
{props.schema}
