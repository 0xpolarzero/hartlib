# Implement the archived chat view

Read `AGENTS.md`, `docs/design.spec.md`, `docs/engineering.spec.md`, the chat
runtime specification, the approved chat-reset artifacts, and the React
guidance available in the repository before editing. Preserve protected files.

Complete the web Archived tab and its discoverable read-only transcript. Apply
the active and archived collection rules, allowed export and deletion actions,
blocked write and sharing actions, and reset invalidation for both active and
archived collections. Keep the UI thin, use the existing API and TanStack
patterns, and add focused authorization and browser coverage where required.

## Plan

{JSON.stringify(props.plan, null, 2)}

## Optional context

{props.prompt || "(none)"}

Do not broaden product scope. Ask for a human only for a protected-file or
destructive/out-of-scope conflict.

REQUIRED OUTPUT:
{props.schema}
