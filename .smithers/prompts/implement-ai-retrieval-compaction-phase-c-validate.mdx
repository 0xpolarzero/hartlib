# Validate Phase C

Do not edit files. Run the complete supplied Phase C command set exactly as
written. Capture real output and exit codes for every command, including
workflow-operation, integration, context, and provider-request tests. Do not
apply migrations or replace the command set.

## Commands

`{JSON.stringify(props.commands)}`

## Context

{JSON.stringify(props.context, null, 2)}

REQUIRED OUTPUT:
{props.schema}
