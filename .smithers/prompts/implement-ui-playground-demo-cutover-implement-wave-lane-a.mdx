# Implement wave lane A

Implement only lane A's assigned work items and tests. Read `AGENTS.md`, the
settled plan, canonical docs, and `docs/references/effect-smol/` before any
Effect v4 backend change. Stay inside the exact files in the assignment. Never
touch `ui-playground/`, unrelated starting changes, another lane's files, or
add a fallback, alias, compatibility layer, mock, placeholder, or dual path.
Run the real focused checks, update affected specifications, and report exact
changes, deletions, tests, evidence, and any true contract exception.

Assignment:

{JSON.stringify(props.assignment, null, 2)}

Frozen contracts:

{JSON.stringify(props.frozenContracts, null, 2)}

REQUIRED OUTPUT:
{props.schema}
