# Implement the reset API and typed client

Use the plan below as the boundary. Read `AGENTS.md`, the relevant canonical
docs, the approved chat-reset artifacts, and `docs/references/effect-smol/`
before editing. Preserve the verified foundation and all protected files.

Implement the reset route, identity and request-schema checks, result
projection, status mapping, mutation audit wiring, and typed API client. Keep
authorization and ownership checks explicit. Match the project's Effect v4
patterns and existing error contracts. Add or update focused tests only when
they belong to this API surface; do not edit unrelated worker runtime code.

## Plan

{JSON.stringify(props.plan, null, 2)}

## Optional context

{props.prompt || "(none)"}

If completion would require changing a protected unrelated file or taking an
out-of-scope or destructive action, stop and set a required human request.
Otherwise return concrete changed-file and acceptance evidence.

REQUIRED OUTPUT:
{props.schema}
