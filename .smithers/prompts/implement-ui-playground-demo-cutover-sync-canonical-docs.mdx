# Sync canonical specifications

Update only affected canonical documents so they describe the final behavior,
architecture, interfaces, commands, migration rules, and project expectations.
Delete stale capability claims. Keep docs as the current specification, not a
history log. Cover the frozen contracts, retained/deleted surfaces, real test
commands, no-skip evidence, and the singular chat and demo reachability rules.

Frozen contracts:

{JSON.stringify(props.frozenContracts, null, 2)}

Implementation cleanup:

{JSON.stringify(props.implementation, null, 2)}

REQUIRED OUTPUT:
{props.schema}
