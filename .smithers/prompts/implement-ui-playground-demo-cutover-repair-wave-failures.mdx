# Repair failed wave checks

Diagnose every failed wave check from its command output. Fix code or tests in
the wave ownership set, using the local Effect v4 references for backend work.
Run the real command again and leave no deferred work. A missing test, skipped
check, warning-only result, mock, placeholder, or fabricated evidence is a
failure to repair, not an acceptable result.

Checks:

{JSON.stringify(props.checks, null, 2)}

Assignment:

{JSON.stringify(props.assignment, null, 2)}

REQUIRED OUTPUT:
{props.schema}
