# Render the final typed report

Do not edit files and do not change readiness. Render only evidence from the
supplied reconciliation. Preserve the initialization scope and acceptance maps,
incremental canonical-doc policy, every phase result, validation and review
history, Phase E preflight, changed path, migration result, acceptance-coverage
item, protected-state result, clean-cutover result, readiness value, and blocker.
Never invent success, omit a failure, or turn an absent evidence item into a pass.

## Reconciled readiness

{JSON.stringify(props.readiness, null, 2)}

REQUIRED OUTPUT:
{props.schema}
