# Repair full-matrix failures

Use the real command logs and matrix output to diagnose every repairable
failure. Fix code, tests, or evidence collection with Luna, following
`AGENTS.md`, the frozen command manifest, and `docs/references/effect-smol/`.
Add missing coverage when the matrix exposes it. Never turn a failure into a
skip, warning-only result, mock, placeholder, or fabricated provider record;
invalidate stale evidence so the whole matrix runs again.

Aggregate:

{JSON.stringify(props.aggregate, null, 2)}

Manifest:

{JSON.stringify(props.manifest, null, 2)}

REQUIRED OUTPUT:
{props.schema}
