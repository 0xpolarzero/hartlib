# Freeze the cutover contracts

Using the repository audit and the complete plan, freeze the exact behavior
rules, interfaces, retained files, deletion map, dependency boundaries,
migration rules, verification gates, and evidence rules. Resolve stale
canonical claims in favor of settled plan decisions and list each resolution.
Do not edit files. Make every rule concrete enough for a worker or a shell
check to enforce. Do not add compatibility aliases, fallback paths, mocks, or
dual implementations.

Repository audit:

{JSON.stringify(props.repositoryAudit, null, 2)}

REQUIRED OUTPUT:
{props.schema}
