{
  "name": "brief",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run db:up && (bun run dev:api & bun run dev:worker & bun run dev:web & wait)",
    "dev:web": "bun --filter @brief/web dev",
    "dev:demo": "bun run db:up && (bun run dev:api & bun run dev:worker & bun --filter @brief/demo dev & wait)",
    "dev:api": "bun --filter @brief/api dev",
    "dev:worker": "bun --filter @brief/worker dev",
    "build": "bun run --filter '*' build",
    "check": "tsc -b --noEmit",
    "lint": "oxlint apps packages tests *.ts",
    "format": "oxfmt AGENTS.md README.md package.json tsconfig.json tsconfig.base.json vitest.config.ts playwright.config.ts apps/*/package.json apps/*/tsconfig*.json apps/*/*.ts apps/*/src packages/*/package.json packages/*/tsconfig*.json packages/*/src tests docs/*.md",
    "format:check": "oxfmt --check AGENTS.md README.md package.json tsconfig.json tsconfig.base.json vitest.config.ts playwright.config.ts apps/*/package.json apps/*/tsconfig*.json apps/*/*.ts apps/*/src packages/*/package.json packages/*/tsconfig*.json packages/*/src tests docs/*.md",
    "test": "bunx --bun vitest run --root .",
    "e2e": "BRIEF_E2E_STACK=1 bunx --bun playwright test --project=brief-ai-chat-runtime --project=brief-platform",
    "eval:ai": "bun apps/worker/src/ai/evaluation/cli.ts",
    "db:up": "docker compose up -d postgres",
    "db:migrate": "bun --filter @brief/worker db:migrate",
    "db:reset": "bun --filter @brief/worker db:reset",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/bun": "1.3.14",
    "@types/node": "25.7.0",
    "esbuild": "0.28.1",
    "oxfmt": "0.56.0",
    "oxlint": "1.71.0",
    "typescript": "6.0.3",
    "vite": "8.1.0",
    "vitest": "4.1.9"
  },
  "patchedDependencies": {
    "@smithers-orchestrator/engine@0.27.0": "patches/@smithers-orchestrator%2Fengine@0.27.0.patch",
    "smithers-orchestrator@0.27.0": "patches/smithers-orchestrator@0.27.0.patch"
  }
}
