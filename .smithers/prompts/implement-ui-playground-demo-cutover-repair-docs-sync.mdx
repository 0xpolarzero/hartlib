# Repair documentation findings

Resolve every valid documentation finding in the affected canonical files.
Keep the docs as the current specification and remove stale claims rather than
adding a dated note or changelog entry. Preserve the settled plan decisions and
make commands and interfaces exact.

Findings:

{JSON.stringify(props.findings, null, 2)}

REQUIRED OUTPUT:
{props.schema}
