# Repair the verification manifest

Add missing real tests or correct commands, service setup, ports, credentials,
and evidence rules. Do not fabricate a secret, downgrade a live-provider flow,
or replace a missing test with deterministic coverage. Keep the six exact visual
widths and the mandatory retrieval baseline. Report every changed file and
remaining external requirement.

Manifest review:

{JSON.stringify(props.review, null, 2)}

REQUIRED OUTPUT:
{props.schema}
