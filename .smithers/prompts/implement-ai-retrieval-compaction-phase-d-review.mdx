# Review Phase D

Do not edit files. Check complete-list manifest validation, stable bounded normal
group concurrency, source-local oversized tools, exact document/chat range proof,
ledger-order merge, one monotone fallback, one final measure, and removal of
`ContextReductionLoop` from direct, topic, and synthesis paths. Verify tests,
safe logging, protected baseline, and behavior against Sections 14–17. This run
may resume with Phase E or F work already present; later-phase files, commits,
and a dirty worktree are not Phase D blockers. Review only defects that break
the Phase D contract. Read the canonical spec and Effect v4 reference. Approve
only with concrete evidence; review rejection blocks the workflow and has no
repair path.

## Plan and validation

{JSON.stringify(props.plan, null, 2)}

{JSON.stringify(props.validation, null, 2)}

REQUIRED OUTPUT:
{props.schema}
