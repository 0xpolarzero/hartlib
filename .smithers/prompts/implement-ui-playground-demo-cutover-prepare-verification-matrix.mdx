# Prepare the complete verification matrix

Give the exact plan-required matrix to the check tasks. Add or extend real
tests for migration 0074, unit and integration behavior, builds, deterministic
Playwright, six visual widths, accessibility, live retrieval, live Stop,
live reset-during-run, reachability and bundles, callers and capabilities,
reference bytes, lint, and root checks. Freeze commands, services, isolated
ports, credential names, and evidence artifacts. Reject mocks, placeholders,
best-effort commands, and skips. The live retrieval baseline must be the
mandated `tests/e2e/chat.spec.ts` title with real provider flags.

Contracts:

{JSON.stringify(props.contracts, null, 2)}

Cleanup:

{JSON.stringify(props.cleanup, null, 2)}

Reserved ports: {JSON.stringify(props.ports)}

REQUIRED OUTPUT:
{props.schema}
