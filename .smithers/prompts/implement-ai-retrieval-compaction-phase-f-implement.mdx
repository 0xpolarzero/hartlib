# Implement Phase F evaluation, fixtures, and canonical contract

Implement only Phase F after migration 0072 succeeds. Read the approved plan,
canonical spec, all prior evidence, and `docs/references/effect-smol/` for any
Effect v4 work. Add evaluation v4 with deterministic provider cases for accept,
replacement, no evidence, fit-first, parallel groups, oversized tools, monotone
fallback, and unfit failure. Add web and demo projection fixtures and update
phase-logging, shared-chat, and stream tests. Rewrite the canonical retrieval,
context, evaluation, migration, and operations sections as the final contract.
Remove every obsolete producer, prompt, schema, config key, test, observation,
alias, compatibility path, and dead export named in the deletion ledger. Do not
leave incomplete code; report real removed paths and doc status.

## Prior evidence and migration

{JSON.stringify(props.plan, null, 2)}

{JSON.stringify(props.previousPhase, null, 2)}

REQUIRED OUTPUT:
{props.schema}
