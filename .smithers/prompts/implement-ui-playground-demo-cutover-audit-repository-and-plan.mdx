# Audit the repository and the approved cutover plan

Read `AGENTS.md`, the complete fixed plan at `{props.planPath}`, every cited
canonical document, `docs/references/effect-smol/`, the current routes and
schemas, tests, dependencies, and the complete current Git state. Do not edit
files. Treat the repository path `{props.repositoryPath}` as fixed and keep all
starting changes protected. Inventory the source, docs, dependency, route,
test, and retained-reference evidence needed by later workers. Record real
contradictions and settled plan overrides instead of guessing. Include the
starting output when it is useful, but do not repeat secrets.

REQUIRED OUTPUT:
{props.schema}
