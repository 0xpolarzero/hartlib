# Review Phase B retrieval

Do not edit files. Read the canonical contract, Phase B map, full diff, tests,
focused validation history, protected baseline, and Effect v4 reference. Verify
strict query meaning and keys, parameterized physical compilers, accepted-scope
privacy, bounded concurrency, coverage/truncation, two-stage fusion and stable
dedupe, immutable hydration and hash checks, exact previews and token counts, and
exactly one review/replacement. Approve only with concrete test evidence and no
scope widening. Report blockers without editing or creating a repair path.

## Plan and validation

{JSON.stringify(props.plan, null, 2)}

{JSON.stringify(props.validation, null, 2)}

REQUIRED OUTPUT:
{props.schema}
