# Repair only Phase F validation failures

Repair only failures caused by Phase F evaluation v4, deterministic fixtures,
projection tests, phase logging, canonical docs, or deletion ledger work. Read
the exact failed results and current diff. Keep the fixed Section 16 command set,
do not widen scope, touch protected changes, or add compatibility aliases. Use
the local Effect v4 reference when needed. Report concrete repairs and blockers.

## Plan and validation

{JSON.stringify(props.plan, null, 2)}

{JSON.stringify(props.validation, null, 2)}

REQUIRED OUTPUT:
{props.schema}
