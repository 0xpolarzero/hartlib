# Recheck canonical documentation

Repeat the read-only document review after repairs. Read every affected
canonical file, compare it with the settled plan and current source, and report
whether all stale capability claims, missing commands, and contract gaps are
gone. Do not edit files or accept a partial document set.

Prior review:

{JSON.stringify(props.priorReview, null, 2)}

REQUIRED OUTPUT:
{props.schema}
