# Review the live cutover dashboard

Perform a read-only browser review of `{props.route}` for workflow key
`{props.workflowKey}`. Inspect the live Gateway run list, stage graph, agent
fleet, every visible AI chat stream, output envelopes, ownership and diffs,
retention/deletion proof, migration evidence, failed versus skipped statuses,
crash-resume state, logs, external blockers, and the approval panel. Check the
route and the six required viewport widths. Use real Gateway data and record
screenshots or route evidence. Do not edit files and do not call a review
successful because a panel is empty when its source should be present.

REQUIRED OUTPUT:
{props.schema}
