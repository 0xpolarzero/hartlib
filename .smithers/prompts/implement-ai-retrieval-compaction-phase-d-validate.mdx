# Validate Phase D

Do not edit files. Run every supplied Phase D command exactly, in order, and
capture real exit codes and failures. The set must cover context, workflow
operations, integration, and phase logging. Do not replace a command, apply a
migration, or hide a failure.

## Commands

`{JSON.stringify(props.commands)}`

## Context

{JSON.stringify(props.context, null, 2)}

REQUIRED OUTPUT:
{props.schema}
