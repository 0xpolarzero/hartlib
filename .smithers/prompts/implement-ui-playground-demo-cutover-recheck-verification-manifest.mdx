# Recheck the verification manifest

Repeat the read-only matrix review after repairs. Confirm complete command and
evidence coverage, no unsafe skips, real credentials, service and port plans,
and the full-stack live retrieval, Stop, and reset flows. Classify only true
external requirements as blockers.

Prior review:

{JSON.stringify(props.priorReview, null, 2)}

Manifest:

{JSON.stringify(props.manifest, null, 2)}

REQUIRED OUTPUT:
{props.schema}
