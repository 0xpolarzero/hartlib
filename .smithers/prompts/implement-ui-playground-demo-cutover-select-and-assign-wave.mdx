# Select and assign the next integration wave

Read the frozen wave plan and the latest checkpoint. Select only work whose
dependencies are complete. Prove exact file-set disjointness for at most three
parallel lanes; if ownership or dependencies overlap, emit one serialized lane.
Never assign `ui-playground/`, starting user changes, or files outside the
frozen ownership record. Include the remaining wave list so the outer loop can
finish without silently dropping work.

Wave plan:

{JSON.stringify(props.wavePlan, null, 2)}

Prior checkpoint:

{JSON.stringify(props.priorCheckpoint, null, 2)}

REQUIRED OUTPUT:
{props.schema}
