# Implement wave lane C

Implement only lane C's assigned work items and tests. Follow `AGENTS.md`, the
settled plan, canonical docs, and `docs/references/effect-smol/` for Effect v4.
Use the exact assigned file set, keep `ui-playground/` byte-for-byte, and do
not change another lane or an unrelated starting file. No mocks, placeholders,
fallbacks, aliases, compatibility layers, or dual paths are allowed. Run real
focused checks and return precise change, deletion, test, and evidence lists.

Assignment:

{JSON.stringify(props.assignment, null, 2)}

Frozen contracts:

{JSON.stringify(props.frozenContracts, null, 2)}

REQUIRED OUTPUT:
{props.schema}
