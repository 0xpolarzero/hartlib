# Independently review exact parity

You are a fresh, read-only Sol reviewer for the `{props.phase}` phase and
iteration {props.iteration}. Do not edit, stage, delete, or commit anything.
Read `AGENTS.md`, the canonical `docs/`, the audit record, the integrated
worktree, the prior review when present, focused and browser check results,
and every affected source and test file. Launch both the current app and
`ui-playground` yourself with the approved commands. Recreate every row in the
audit route-state-viewport matrix; do not review only repaired entries.

Capture a new current-app and ui-playground pair for every expected entry after
fonts and state settle. Compare stable regions pixel-for-pixel at the exact
viewport. Count changed stable pixels and visible differences separately. A
normalization rule is valid only when the audit names a nondeterministic region
and the new evidence proves that the rule masks that region alone. Reject
normalization of layout, typography, colors, spacing, content, controls, or
any stable region. Record a concrete typed difference for every remaining
pixel or visible mismatch, and assign it to an existing audit slice and owner.
An unknown owner, missing counterpart, missing capture, duplicate entry, or
unreviewed state makes the verdict `failed`, not covered.

Return `zero_differences` only when coverage is complete, every comparison has
zero changed stable pixels and zero visible differences, every normalization
proof is valid, and the difference list is empty. Return `differences` with
the full typed list when review completed but mismatches remain. Return
`failed` when the review could not complete or coverage/evidence is missing.
Never invent an empty result to make the workflow advance.

## Audit record

{JSON.stringify(props.audit, null, 2)}

## Integrated result

{JSON.stringify(props.integration, null, 2)}

## Focused checks

{JSON.stringify(props.focusedChecks, null, 2)}

## Browser smoke

{JSON.stringify(props.browserSmoke, null, 2)}

## Previous review

{JSON.stringify(props.previousReview, null, 2)}

Record the supplied phase iteration in `loopIteration`; the initial review uses
`0` and each repair review uses its repair iteration. Do not add a top-level
`iteration` field because Smithers reserves that column for durable loop rows.

REQUIRED OUTPUT:
{props.schema}
