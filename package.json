{
  "name": "hartlib",
  "private": true,
  "description": "A single chat for your subscriptions and densest sources: grounded answers, explained the way you understand best.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun scripts/dev.ts",
    "dev:web": "bun scripts/dev.ts",
    "dev:api": "bun --filter @hartlib/api dev",
    "dev:worker": "bun --filter @hartlib/worker dev",
    "build": "bun run --filter '*' build",
    "check": "tsc -b --noEmit",
    "lint": "oxlint apps packages scripts tests *.ts",
    "format": "oxfmt AGENTS.md README.md package.json tsconfig.json tsconfig.base.json vitest.config.ts playwright.config.ts scripts/*.ts scripts/tsconfig.json apps/*/package.json apps/*/tsconfig*.json apps/*/*.ts apps/*/src packages/*/package.json packages/*/tsconfig*.json packages/*/src tests docs/*.md",
    "format:check": "oxfmt --check AGENTS.md README.md package.json tsconfig.json tsconfig.base.json vitest.config.ts playwright.config.ts scripts/*.ts scripts/tsconfig.json apps/*/package.json apps/*/tsconfig*.json apps/*/*.ts apps/*/src packages/*/package.json packages/*/tsconfig*.json packages/*/src tests docs/*.md",
    "test": "bun scripts/test-with-postgres.ts",
    "e2e": "HARTLIB_E2E_STACK=1 bunx --bun playwright test --project=hartlib-ai-chat-runtime",
    "eval:ai": "bun apps/worker/src/ai/evaluation/cli.ts",
    "db:up": "docker compose up -d postgres",
    "db:migrate": "bun --filter @hartlib/worker db:migrate",
    "db:reset": "bun --filter @hartlib/worker db:reset",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "1.61.1",
    "@types/bun": "1.3.14",
    "@types/node": "25.7.0",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "esbuild": "0.28.1",
    "oxfmt": "0.56.0",
    "oxlint": "1.71.0",
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "typescript": "6.0.3",
    "vite": "8.1.0",
    "vitest": "4.1.9"
  }
}
