# Reconcile the final parity proof

You are the read-only Sol proof owner. Do not edit, stage, delete, or commit
anything. Read the complete current worktree, `AGENTS.md`, canonical `docs/`,
the audit, all integration and repair records, every independent review, all
focused and browser checks, and the final artifact paths. Inspect the final
git status and recompute the `ui-playground/` byte-tree fingerprint plus every
protected user-change fingerprint.

Build one complete proof bundle. Carry the full audit coverage matrix forward
and mark every inventory item resolved at the iteration and owner that fixed
it, or keep it in `remainingDifferences` with its evidence. Verify exact
review coverage: expected, reviewed, missing, duplicate, stable changed
pixels, visible differences, and valid normalization proofs. Verify that the
repair history contains no more than the requested bound and that each repair
reported only the latest review's differences. Verify every focused and
browser command passed without a skip, both guards stayed intact, ownership
has no overlaps or unowned files, and docs reflect any behavior or contract
change. Preserve all artifact paths; never replace missing evidence with a
claim.

Set `status` to `passed` only when the terminal review is
`zero_differences`, coverage is complete, every comparison has zero stable
changed pixels and zero visible differences, every normalization proof is
valid, all required checks passed without skips, ownership is clean, docs are
accurate, `ui-playground` is unchanged, and prior user changes are preserved.
Otherwise set `status` to `failed` and retain every remaining difference and
its evidence. The proof must still be complete on failure so the enforcing
node can terminate with a useful typed record.

## Audit

{JSON.stringify(props.audit, null, 2)}

## Initial review

{JSON.stringify(props.initialReview, null, 2)}

## Terminal repair review

{JSON.stringify(props.repairReview, null, 2)}

## Validation and smoke evidence

Initial checks: {JSON.stringify(props.initialChecks, null, 2)}

Initial browser smoke: {JSON.stringify(props.initialBrowser, null, 2)}

Final checks: {JSON.stringify(props.finalChecks, null, 2)}

Final browser smoke: {JSON.stringify(props.finalBrowser, null, 2)}

Maximum repair iterations: {props.maxRepairIterations}

REQUIRED OUTPUT:
{props.schema}
