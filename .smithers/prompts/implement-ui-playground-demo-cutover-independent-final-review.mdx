# Perform the independent final review

Read the full approved plan, frozen contracts, canonical docs, current
repository state, deletion and ownership maps, retained-reference baseline,
verification evidence, and prior resolutions. Review without editing. Confirm
singular chat and demo reachability, destructive migration 0074 with no old
path, no shipped `ui-playground/`, no obsolete capability, no skipped or
warning-only check, exact six-width evidence, live-provider proof, and stable
repository digest. List every valid finding with a required fix.

Plan: `{props.planPath}`

Contracts:

{JSON.stringify(props.contracts, null, 2)}

Verification aggregate:

{JSON.stringify(props.aggregate, null, 2)}

Retention:

{JSON.stringify(props.retention, null, 2)}

REQUIRED OUTPUT:
{props.schema}
