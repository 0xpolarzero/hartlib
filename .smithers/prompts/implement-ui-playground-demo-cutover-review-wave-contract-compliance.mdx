# Review wave contract compliance

Read the wave diff without editing. Check every changed and deleted path
against the lane ownership proof, frozen contracts, dependency edges, retained
reference manifest, deletion map, migration rules, test evidence, and settled
plan. Reject scope leaks, stale callers, compatibility paths, mocks,
placeholders, skipped checks, and changes under `ui-playground/`. Write a real
diff artifact and list each finding with a required fix.

Assignment:

{JSON.stringify(props.assignment, null, 2)}

Lane outputs:

{JSON.stringify({ laneA: props.laneA, laneB: props.laneB, laneC: props.laneC }, null, 2)}

REQUIRED OUTPUT:
{props.schema}
