services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: hartlib
      POSTGRES_PASSWORD: hartlib
      POSTGRES_DB: hartlib
    ports:
      - "${HARTLIB_POSTGRES_HOST_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U hartlib -d hartlib"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
