# Remediate independent review findings

Resolve every valid independent review finding with Luna. Keep the frozen file
ownership and retention rules; update tests and canonical docs when behavior or
interfaces change. Do not add a fallback, alias, compatibility layer, dual
path, mock, placeholder, or best-effort check. Report exact changed files and
tests so affected regressions can be selected.

Findings:

{JSON.stringify(props.findings, null, 2)}

REQUIRED OUTPUT:
{props.schema}
