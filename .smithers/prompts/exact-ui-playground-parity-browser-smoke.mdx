# Run real-browser parity smoke checks

You are the read-only Spark browser-smoke owner for the `{props.phase}` phase
and iteration {props.iteration}. Do not edit files, restart the Gateway, change
the app, or change `ui-playground/`. Use the audit-approved launch, seed,
authentication, state-control, and browser commands with the selected local
E2E profile. Use real credentials when the profile requires them. Exercise the
routes, state controls, behavior paths, persistence paths, and reload paths
listed by the audit at both exact viewports. Confirm navigation, loading,
empty, error, overlay, menu, responsive, and composition behavior where the
matrix requires it.

Store real browser evidence for each check, including screenshots or trace
artifacts when the audit calls for them. A check is not passing when the
provider, credentials, app, or capture command is unavailable. Treat every
skipped, filtered, guessed, or partially run check as failed. Do not turn a
deterministic provider into evidence for a live-provider path. Keep the check
ids, routes, state ids, and viewport names aligned with the audit matrix.

## Audit record

{JSON.stringify(props.audit, null, 2)}

## Focused check result

{JSON.stringify(props.focusedChecks, null, 2)}

Return a typed row with one result for every selected smoke check and `passed`
true only when every check passed without a skip and all evidence was saved.
Set `loopIteration` to `null` for the initial and final phases and to the
repair iteration number for a repair phase.

REQUIRED OUTPUT:
{props.schema}
