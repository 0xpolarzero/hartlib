# Review Phase A

Do not edit files. Independently read the plan, Sections 14–17, canonical spec,
all Phase A changes, focused validation output, complete diff, protected baseline,
and the local Effect v4 reference. Check strict contracts, canonical and local
identity separation, deterministic passage/range proof, RRF provenance and ties,
manifest completeness, tests, clean-cutover boundaries, and scope. Approve only
when the evidence is concrete and no later-phase behavior slipped into Phase A.
List each blocker with a useful file or test reference; do not propose a review
repair loop.

## Plan and validation

{JSON.stringify(props.plan, null, 2)}

{JSON.stringify(props.validation, null, 2)}

REQUIRED OUTPUT:
{props.schema}
